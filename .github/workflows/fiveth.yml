name: Fifth Workflow
on: [push, issues, pull_request]

jobs:
  run-fifth-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Events
        run: |
          node -v
          npm run build
