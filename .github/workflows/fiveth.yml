name: Fifth Workflow
on: [push, issues, pull_request]

jobs:
  run-fifth-job:
    runs-on: ubuntu-latest
    steps:
      - name: Events
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
