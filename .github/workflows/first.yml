name: First work
on: [push]

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: Math test
        run: expr 2+5 > result_expr.txt
      - name: Upload doc
        uses: actions/upload-artifact@v3
