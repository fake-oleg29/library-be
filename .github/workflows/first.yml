name: First work
on: [push]

jobs:
  run-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: autor
        run: echo ${{github.actor}}
      - name: ref_name
        run: echo ${{github.ref_name}}
      - name: current date
        run: date
