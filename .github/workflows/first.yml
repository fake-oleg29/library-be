name: First work
on: [push]

jobs:
  run-init:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: orest
        if: github.actor == 'orest'
        run: echo "It is orest"
      - name: actor
        if: github.actor == 'fake-oleg29'
        run: |
          echo "It is fake-oleg29"
          echo ${{github.env}}
