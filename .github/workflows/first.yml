name: First work
on: [push]

jobs:
  run-init:
    runs-on: ubuntu-latest
    environment: "dev"
    steps:
      - name: SECRET & VAR test
        run: |
          echo ${{vars.TEST}}
          echo ${{secrets.JWR}}
