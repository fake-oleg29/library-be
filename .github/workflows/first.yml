name: First work
on: [push]

env:
  SSO_TYPE_VAR: SAML

jobs:
  run-init:
    runs-on: ubuntu-latest
    env:
      TEST: 12
    steps:
      - name: Event vars
        run: |
          echo $GITHUB_REF
          echo ${{env.TEST}}
          echo ${{env.SSO_TYPE_VAR}}
