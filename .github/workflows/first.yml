name: First work
on: [push]

jobs:
  run-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: node vertion
        run: node -v
  run-second-job:
    runs-on: ubuntu-latest
    needs: [run-first-job]
    steps:
      - name: start
        run: |
          npm i
          npm run build
